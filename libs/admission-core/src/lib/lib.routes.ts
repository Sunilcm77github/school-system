import { Route } from '@angular/router';
import { AdmissionCoreComponent } from './admission-core/admission-core.component';

export const admissionCoreRoutes: Route[] = [
  { path: '', component: AdmissionCoreComponent },
];
