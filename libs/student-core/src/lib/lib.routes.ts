import { Route } from '@angular/router';
import { StudentCoreComponent } from './student-core/student-core.component';

export const studentCoreRoutes: Route[] = [
  { path: '', component: StudentCoreComponent },
];
